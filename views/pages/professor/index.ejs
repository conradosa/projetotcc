<div class="row row-height">
    <div class="col-xl-4 col-lg-4 content-left">
        <div class="content-left-wrapper">
            <div>
                <h2>Olá, <%= req.session.usuarioNome; %>!</h2>
                <p>Esta é a página do professor, aqui você pode acessar o seu fluxo de etapas para saber em qual etapa
                    da entrega do TCC você se encontra. Além de ter acesso às sugestões de temas na planilha alimentada
                    pelo campus.</p>
            </div>
        </div>
    </div>
    <div class="col-xl-8 col-lg-8 content-right">
        <div class="text-center" id="wizard_container">
            <div class="row d-flex justify-content-center">
                <button style="width: 50%;" onclick="window.location.replace('<%= sails.getUrlFor('ProfessorController.listarAlunos') %>')"
                        class="submit">Meus alunos
                </button>
            </div>
            <br>
            <div class="row d-flex justify-content-center">
                <% if(disponivel){ %>
                    <form method="POST" action="<%= sails.getUrlFor('ProfessorController.disponivel') %>">
                        <label class="cl-switch custom-class">
                            <input checked onChange="this.form.submit()" type="checkbox">
                            <span class="switcher"></span>
                            <span class="label">Estou disponível</span>
                        </label>
                    </form>
                <% }else{ %>
                    <form method="POST" action="<%= sails.getUrlFor('ProfessorController.disponivel') %>">
                        <label class="cl-switch custom-class">
                            <input onChange="this.form.submit()" type="checkbox">
                            <span class="switcher"></span>
                            <span class="label">Estou disponível</span>
                        </label>
                    </form>
                <% } %>
            </div>
        </div>
    </div>
</div>
